{
  "name": "amplify-stage",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ amplify-labs/amplify-stage --access-token ${GRAPH_TOKEN}",
    "create-local": "graph create --node http://localhost:8020/ amplify-labs/amplify-stage",
    "remove-local": "graph remove --node http://localhost:8020/ amplify-labs/amplify-stage",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs https://api.thegraph.com/ipfs/ amplify-labs/amplify-stage"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.20.1",
    "@graphprotocol/graph-ts": "0.20.0"
  }
}